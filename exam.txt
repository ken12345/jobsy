1 ccreata react pjoject and named it reactExam
2 clear the main.css and app.css
3 clear content of from App.tsx (make sure only function App, UI renderer and its export will only be retained)
4 create 2 pages and 1 Card component
5. Card component should have 3 properties{name, age, status, and id}
6 add 3 divs on Card component and use the properties as it's data. Make sure It has labels before the data (ex: Name: Ken)
5. import the data.json

6. the component properties of Card should be coming from the data.json
7. add button, labeled it "View" on Card component